# xkill command works to kill a "process" but in this case, graphically.

# so, in other words, in a GUI environment with a Window manager running. So if you type:

# xkill

# it will prompt you to select a window to kill, and once you click on that window, it'll terminate it
