#!/bin/bash

someWord=tEsT

echo ${someWord^} # Converts firt letter to Uppercase
echo ${someWord^^} # Converts all the word/sentence to Uppercase 

echo ${someWord,} # Convert first letter to Lowercase
echo ${someWord,,} # Converts all the word/sentence to Lowercase
