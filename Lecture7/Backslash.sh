#!/bin/bash

echo "\"Linux is Awesome\""

# You can use the escape char in grep as well, like for example: ls --help | grep "\-U" 
# If you omit the "\" it will treat -U as an option to grep, but what we want is to literally search for the -U option and what it does.

